# Module manifest for Azure Functions managed dependencies.
# Flex Consumption does not support managed dependencies — use Save-Module
# to vendor modules into the Modules/ folder instead.

@{
#    'Az' = '14.*'
}
